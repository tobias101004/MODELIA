[phases.setup]
nixPkgs = ["python39", "nodejs"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[phases.build]
cmds = ["mkdir -p temp static"]

[start]
cmd = "uvicorn main:app --host 0.0.0.0 --port $PORT"
